npm install --global gulp-cli
npm install --save-dev gulp
Запуск.
открываем в cmd проект, далее запускаем gulp watch
Вопросы.
1. Как в целом?
2. За сколько это может сверстать ч-к с опытом? мне удалось это сделать за 4 дня(на 4 день я занималась уже jquery).
3. Мне кажется много получилось всего избыточного  по верстке. например, где-то я дублировала код. надо было создать какой-нибудь стилус-файлик  с конструкциями, которые постоянно повторяются и при повторении брать класс оттуда и все?
4. вот бесило прям. верстаешь 2 элемента, и у них много общего, например шрифт и цвет, но размеры шрифта разные. надо было написать какую-нить функцию цсс для этого? а можно мини-пример?
5. верстка выпадающих списков. например <select id="Newsletter"><option>Newsletter</option></select>. у меня значок и выпадашка не "приклеены"к списку, то есть, если список сдвинуть, то элементы останутся на своих местах. как их надо было все склеить? также я же убрала кастомный переключатель и на его место другой вставила и по нажатию на него, у меня не раскарывается выпадашка. а как нужно было сделать правильно?
6. я замучилась все писать в 1 файле-pug. А его можно было разделить?
7. <div class="menu">, как его надо было правильно продлить только в 1 сторону - вправо? как делать в обе стороны, мне понятно, а в одну нет.
8. <div class="wrapper-info-bloggers"> в нем еще написано About us. Ну почему он съехал немного вправо по сравнению с остальными блоками (котоыре под ним). что я сделала не так?!
9. в чем магия line-height? смысл свойства не поняла, но подгоняла мне текст как в фотошопе.
=====================
Моменты. 
1. надо поправить выпадашки.
2. фиксировать минимальную ширину, чтобы верстка не разъежалась
3. надо картинки сохранять в свг формате - всякие малкенькие значки, если они векторные, или растягивать картинку в 2 раза, а потом ужимать нужно для ретины дисплея
div.select //css relative
	div.title
		span Newsletter
		div.arrows.event_click_open_list
	div.list (скрыта) //css absolute top,etc.
		span news 1 click//list.hide
		span news 2

event_click_open_list.click
	$('.select .list').show()